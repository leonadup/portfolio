[project]
name = "voronoi_app"
version = "0.1.0"
description = "Voronoi diagram generator with SVG export and benchmarking."
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra"
testpaths = ["tests"]
